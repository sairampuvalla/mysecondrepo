---
- name: get the hostname
  gather_facts: no
  hosts: localhost
  tasks:
    - name: get me the hostname
      shell: adabdsan
      register: result
    - debug:
        msg: "{{ result.stdout }}"
